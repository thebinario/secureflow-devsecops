# SecureFlow Examples

This directory is split into two areas:

- `consumers/`: copyable workflows for external repositories that consume SecureFlow.
- `fixtures/`: intentionally insecure local fixtures and validation workflows for testing SecureFlow in a controlled environment.

Use `consumers/` when you want to onboard another repository.

Use `fixtures/` when you want to validate scanner behavior locally inside a controlled lab environment.
