# SecureFlow Advanced Consumers

Copy one of these workflows into the target repository at:

```text
.github/workflows/secureflow-ci.yml
```

These variants are intended for repositories that need narrower or stricter SecureFlow gates.
