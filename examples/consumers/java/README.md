# SecureFlow Java Consumer

Copy one of these workflows into the target repository at:

```text
.github/workflows/secureflow-ci.yml
```

Use `maven.yml` for Maven repositories and `gradle.yml` for Gradle repositories.
