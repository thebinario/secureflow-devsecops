# SecureFlow .NET Consumer

Copy this workflow into the target repository at:

```text
.github/workflows/secureflow-ci.yml
```

This example is intended for .NET application and service repositories.
