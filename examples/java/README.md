# SecureFlow Java Examples

This entry groups the Java examples for Maven and Gradle projects.

## Consumer workflows

Copy one of the consumer workflows into the target repository at:

```text
.github/workflows/secureflow-ci.yml
```

- `../consumers/java/maven.yml`
- `../consumers/java/gradle.yml`

## Local fixtures

Use these fixtures to validate SecureFlow locally in this repository:

- `../fixtures/java-maven-vulnerable-app`
- `../fixtures/java-gradle-vulnerable-app`

## Community lab coverage

For Java community lab validation, use OWASP WebGoat from:

```text
../fixtures/docker-compose.vulnerable-labs.yml
```
